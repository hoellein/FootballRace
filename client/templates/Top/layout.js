/**
 * Created by don on 5/14/15.
 */

Template.layout.helpers({

});

//TODO - this could be refactored to a single event
Template.layout.events({
/*     'click #pillConfig': function(evt) {
        //console.log('Config clicked');
/!*        var el = document.getElementsByClassName('pill');
        for(var ii=0; ii<el.length; ii++) {
            el[ii].className -= ' active';
        }
        //document.getElementsByClassName('pill').className -= 'active';
        evt.currentTarget.className += ' active';*!/
        Router.go('configPage');
    }*/
});